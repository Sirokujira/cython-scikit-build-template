# .readthedocs.yml
name:
  cython_template

type:
  sphinx

base:
  docs/source

requirements_file:
  docs/requirements.txt

build:
  image: latest

conda:
  file: environment.yml

python:
  setup_py_install: true
